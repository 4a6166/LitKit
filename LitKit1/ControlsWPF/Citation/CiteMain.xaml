<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LitKit1.ControlsWPF.Citation"
             xmlns:Controls="clr-namespace:LitKit1.Controls" x:Class="LitKit1.ControlsWPF.Citation.CiteMain"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="300">
    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="63"/>
            <RowDefinition Height="45*"/>
            <RowDefinition Height="200*"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="5"/>
        </Grid.RowDefinitions>

        <ScrollViewer 
            Grid.Column="1" Grid.ColumnSpan="2" 
            Grid.Row="2" 
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            VerticalScrollBarVisibility="Auto" 
            HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="CiteBlockStackPanel" 
                        HorizontalAlignment="Left" 
                        VerticalAlignment="Top"
                        />
        </ScrollViewer>
        <Border x:Name="CiteAddedBorder"
                Grid.Column="1" Grid.ColumnSpan="2" 
                Grid.Row="1" Grid.RowSpan="3" 
                BorderBrush="Black" BorderThickness="2" 
                CornerRadius="15, 15, 0, 0" 
                Visibility="Collapsed">
            <local:CiteAdd/>
        </Border>
        <!--<local:CiteFormat HorizontalAlignment="Left" Height="100" VerticalAlignment="Top" Width="100" Visibility="Collapsed" Grid.ColumnSpan="2" Grid.RowSpan="2"/>-->
        <StackPanel x:Name="SearchStackPanel"  Orientation="Vertical"  Grid.Column="1" Grid.ColumnSpan="2" Height="26" Grid.Row="1" Width="auto" HorizontalAlignment="Center" >
            
            <StackPanel x:Name="CiteTypeStackPanel" Orientation="Horizontal" VerticalAlignment="Center">
                <Rectangle Fill="Green" Height="20" Width="60" Margin="3"/>
                <Rectangle Fill="Green" Height="20" Width="60" Margin="3"/>
                <Rectangle Fill="Green" Height="20" Width="60" Margin="3"/>
            </StackPanel>
        </StackPanel>



    </Grid>
</UserControl>
